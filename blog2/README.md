# 프로젝트 : Developer Portfolio Blog

---

## 1. 프로젝트 소개
개발자로서의 정체성, 보유 기술, 주요 프로젝트를 인터랙티브하게 표현하여 전문성을 홍보하는 개인 포트폴리오 정적 블로그 서비스입니다.

---

## 2. 프로젝트 개요
- **제작 배경**: 개발자로서의 활동과 기술 역량을 체계적으로 정리하기 위해 시작했습니다.
- **기획 의도**: 방문자에게 전문적인 인상을 주고 다양한 디바이스에서 최적화된 포트폴리오를 제공하고자 했습니다.
- **프로젝트 목표**: 모던한 UI 디자인과 반응형 웹 기술을 활용하여 높은 가독성과 사용자 편의성을 갖춘 포트폴리오 웹사이트 구축을 목표로 합니다.

---

## 3. 주요 기능
- **동적 내비게이션**: 스크롤 상태에 따라 내비게이션 바의 크기가 축소/확장되어 사용자 경험을 최적화합니다.
- **포트폴리오 모달 상세 보기**: 각 프로젝트 항목 클릭 시 모달창을 통해 상세 설명 GitHub 링크 PDF 자료를 제공합니다.
- **스킬 스택 시각화**: 보유한 스킬들을 모달과 함께 상세히 설명하여 기술 역량을 명확히 보여줍니다.
- **반응형 웹 지원**: 모바일에서 데스크톱 환경까지 다양한 디바이스에 완벽하게 대응하는 레이아웃을 제공합니다.

---

## 4. 기술 스택
- **Markup**: Pug (HTML Preprocessor)
- **Styling**: CSS (Bootstrap 5)
- **Script**: JavaScript (ES6+), jQuery
- **Icons**: Font Awesome, XEIcon
- **Fonts**: Google Fonts (Montserrat, Lato)

---

## 5. 기술 선택 이유
- **Pug**: 믹스인(Mixin) 기능을 통해 HTML 코드를 컴포넌트화하여 유지보수성을 극대화했습니다.
- **Bootstrap 5**: 검증된 레이아웃 컴포넌트를 활용하여 개발 속도를 향상하고 일관된 디자인을 유지합니다.
- **jQuery**: 복잡한 DOM 조작과 스크롤 이벤트 처리를 직관적인 코드로 구현했습니다.

---

## 6. 시스템 아키텍처
- 별도의 백엔드 서버 없이 동작하는 고성능 정적 웹사이트입니다.
- `src/` (소스) -> `build/` (컴파일 결과물) 구조를 통해 배포 효율성을 높였습니다.

```mermaid
graph LR
    A[Source (src/)] -->|Compile/Build| B(Build (build/))
    B -->|Deploy| C[Web Browser]
```

---

## 7. 프로젝트 폴더 구조
```text
blog2/
├── README.md
├── build/
│   ├── css/
│   ├── html/
│   └── script/
├── public/
│   ├── favicon/
│   └── img/
└── src/
    ├── css/
    ├── pug/
    └── script/
```

---

## 8. 트러블 슈팅
- **문제**: 포트폴리오 항목 증가에 따른 HTML 코드 비대화 및 가독성 저하
- **원인**: 반복적인 모달 구조 마크업이 전체 가독성을 해침
- **해결**: Pug의 `mixin` 기능을 도입하여 `portfolio-item`과 `portfolio-modal`을 모듈화하여 코드 양을 80% 이상 절감함.

---

## 9. 성능 개선
- **성능 개선 포인트**: 모든 이미지에 `loading='lazy'` 속성을 적용하여 초기 페이지 로딩 속도 최적화.
- **측정 방법**: Google Chrome DevTools의 Lighthouse Audit 및 Network 탭을 활용하여 측정.
- **결과 (예상)**:
  - **개선 전**: 페이지 로딩 시 모든 포트폴리오 이미지 다운로드 (약 3.5MB 전송)
  - **개선 후**: 뷰포트에 보이는 이미지만 우선 다운로드 (약 0.8MB 전송, 약 77% 데이터 절감)
  - **효과**: 첫 화면 렌더링 완료 속도(LCP) 향상 및 불필요한 네트워크 자원 소비 방지.

---

## 10. 실행 및 테스트 방법
- **로컬 환경 요구사항**:
  - Node.js (v16 이상 권장)
  - 웹 브라우저 (Chrome, Edge 등 최신 버전)
- **설치 및 실행 명령어**:
  - 이 프로젝트는 정적 사이트이므로 별도의 `npm install`은 필요하지 않으나, 소스 수정 및 빌드 시 다음을 수행합니다:
    ```bash
    # 의존성 패키지 설치
    npm install
    # 개발 모드 빌드 및 실행
    npm run dev
    ```
- **테스트 실행 방법**:
  - 브라우저 개발자 도구(F12)의 'Network' 탭을 통해 이미지 Lazy Loading 작동 여부 확인.
  - Lighthouse Audit을 통해 Performance 점수 측정 및 개선 효과 확인.
