name: Shopping Mall CI/CD

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Grant execute permission for mvnw
      run: chmod +x shoppingmall/backend/mvnw
    
    # 1. 백엔드 빌드 (테스트 생략)
    - name: Build Backend
      run: |
        cd shoppingmall/backend
        ./mvnw clean package -DskipTests -Dmaven.test.skip=true

    # 2. Render 배포 트리거 (이제 실제 주소로 배포 신호를 보냅니다)
    - name: Trigger Render Deployment
      run: |
        echo "Sending deployment signal to Render..."
        curl -X POST https://api.render.com/deploy/srv-d6pr7l2a214c73earr9g?key=JOCKRAFCEwQ

    # 3. 배포 확인 로그
    - name: Confirm Status
      run: |
        echo "CI/CD process completed. Vercel and Render are updating automatically."
