# Local 환경에서 사용할 설정 파일입니다.
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

# 로컬 개발 시에는 환경 변수가 없을 경우를 대비한 추가 설정 가능
# 현재는 application.yml의 기본값으로 충분함
